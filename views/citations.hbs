<h1>Index</h1>
<nav>
    <a href="/">Index</a>
    <a href="/research_papers">Research Papers</a>
    <a href="/citations">Citations</a>
    <a href="/authors">Authors</a>
    <a href="/research_papers_authors">Research Papers to Authors</a>
    <a href="/institutions">Institutions</a>
    <a href="/disciplines">Disciplines</a>
</nav>

<table>
    <thead>
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>
    <tbody>
        {{#each data}}
        <tr>
            <td>{{this.citation_id}}</td>
            <td>{{this.citing_paper_id}}</td>
            <td>{{this.cited_paper_id}}</td>
            {{!-- <td><button>Edit</button></td>
            <td><button>Delete</button></td> --}}
        </tr>
        {{/each}}
    </tbody>
</table>

<h2>Add a citation</h2>
<p>To add a new citation, please enter its information below and click 'Submit'!</p>
<form>
    <legend>Add citation</legend>
    <label for="input-citing-paper">Citing Paper</label>
    <select name="input-citing-paper" id="input-citing-paper">
        <option value="">Select a citing paper</option>
        {{#each citing_papers}}
        <option value="{{this.id}}">{{this.title}}</option>
        {{/each}}
    </select>

    <label for="input-referenced">Referenced</label>
    <select name="input-referenced" id="input-referenced">
        <option value="">Select a referenced paper</option>
        {{#each referenced_papers}}
        <option value="{{this.id}}">{{this.title}}</option>
        {{/each}}
    </select>
    <input type="submit">
</form>